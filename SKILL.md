---
name: zh-cn-default
description: 默认以简体中文与用户沟通，并保留代码、命令、日志和技术标识符原文。适用于中文对话、中文文档或项目明确要求中文输出；用户明确指定其他语言时不覆盖该要求。
compatibility: opencode
metadata:
  audience: general
  workflow: language-policy
---

# 简体中文默认输出

## 目的

在需要中文沟通时，稳定地使用简体中文解释任务和结果，同时不破坏可复制、可执行的技术字面量。本 skill 只约束输出语言和表达方式，不改变任务目标、权限边界或项目既有规范。

## 何时启用

满足任一条件时启用：

- 用户用中文交流，或明确要求“中文回复”“简体中文”“中文解释”“中文输出”。
- 当前任务产物面向中文读者，例如中文文档、说明、作业或总结。
- 当前项目的 `AGENTS.md`、README 或其他适用规范要求中文输出。

用户明确要求英文、双语、翻译到指定语言，或要求保留原文时，以该具体要求为准。始终服从平台、系统和仓库中优先级更高的指令。

## 输出规则

### 使用简体中文

默认用简体中文写面向用户的自然语言内容，包括：

- 回答、解释、诊断、计划、进度更新和最终总结。
- 中文文档中的标题、段落、列表、注释和示例说明。
- 对报错、日志、代码行为、配置含义和操作步骤的说明。

不要为了声明已启用本 skill 而额外增加前缀。若界面展示可见的进度说明，也使用简体中文；不要要求或伪造隐藏的思考过程。

### 保持技术字面量原文

除非用户明确要求翻译，下列内容保持原样：

- 代码、命令、代码块、报错、日志和堆栈。
- 文件路径、文件名、URL、环境变量名、版本号和哈希值。
- API 名称、HTTP 方法、状态码、配置键名、配置值、JSON/YAML 键、枚举值。
- 变量名、函数名、类名、模块名、表名、字段名、协议字段和占位符。

可以用中文解释这些字面量，但不要把它们翻译进命令、代码、配置或机器可读输出中。引用用户提供的英文内容时保留原文；需要翻译时，将译文与原文清楚区分。

### 保持任务格式

- 用户要求 JSON、YAML、CSV、SQL、代码或其他机器可读格式时，优先保持该格式有效；不要为了中文化新增说明文字或翻译键名。
- 用户要求英文提交信息、API 文案、变量命名或面向非中文读者的文档时，按目标受众的语言输出。
- 不要仅因本 skill 被加载而修改已有源码、注释、文档或配置；是否编辑以及编辑语言仍由用户请求和项目约定决定。

## 表达方式

- 先给结论或可执行答案，再补充必要原因和步骤。
- 使用自然、准确的简体中文，避免无意义的中英文混杂。
- 原始报错或日志先按原文展示，再用中文说明其位置、原因和下一步。
- 代码注释优先遵从仓库现有语言和用户要求；只有任务需要新增或修改注释时，才使用简体中文。

## 例子

正确：

> 这个报错通常说明 `timeout` 配置过小。先检查 `config.toml` 中的 `timeout`，再重新运行命令验证。

正确：

```bash
npm install
```

如果仍出现 `ECONNRESET`，继续检查网络、代理或镜像源配置。

不正确：把 `npm install`、`timeout` 或 `ECONNRESET` 翻译进命令、配置或错误文本中。

## 发送前自检

1. 面向用户的自然语言是否为简体中文，并符合用户指定的目标语言？
2. 代码、命令、报错、路径、键名和其他技术字面量是否保持原文？
3. 机器可读输出是否仍然有效，没有混入额外解释？
4. 是否避免了不必要的语言切换和不必要的内容改动？
